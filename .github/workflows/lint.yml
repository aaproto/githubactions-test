name: lint

on:
  workflow_call:

jobs:
  lint:
    name: Lint code
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main

    - name: Lint code
      run: |
        ./scripts/lint.ps1